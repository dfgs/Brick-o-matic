<UserControl x:Class="Brick_o_matic.Viewer.Views.ProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:Brick_o_matic.Viewer.Views"
			 xmlns:viewModels="clr-namespace:Brick_o_matic.Viewer.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="1*"/>
			<RowDefinition Height="5"/>
			<RowDefinition Height="2*"/>
		</Grid.RowDefinitions>


		<Viewport3D Grid.Row="0" Camera="{Binding Camera}">
			<Viewport3D.Children>
				<ModelVisual3D Content="{Binding ModelVisual.Content}"/>
			</Viewport3D.Children>
		</Viewport3D>


		<GridSplitter Grid.Row="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>

		<views:TextEditorView  Grid.Row="2"/>
	</Grid>
	
</UserControl>
